#include<iostream>
#include <vector>
/*
	author:Alex
	problem number: 638
	level: medium
*/
using namespace std;

int shoppingOffers(vector<int>& price, vector<vector<int>>& special, vector<int>& needs) {
	int n = price.size() + 1;
	int spNum = special.size();
	int i=0, j=0;
	int cost = 0;
	for (i = 0; i < spNum; ++i) {
		for (j = 0; j < n-1; ++j) {
					
		}
	}
}

int main()
{

	return 0;
}
